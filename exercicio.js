const { cpfs, ips } = require("./base");

// Encontrar os CPFs

const regexp1 = /\./gi;

console.log(cpfs.match(regexp1));

//------------------------------------------------------

// Encontrar os IPs
// const regexp2 = /teste/gi;

// console.log(ipValues)
